# Local demo / development example.
# Do not reuse these values for shared, staging, or production environments.

DATABASE_URL=postgresql://halebopp:halebopp_dev@localhost:5432/halebopp
HB_DICTIONARY_PATH=./data/db-data-dictionary.json

# Optional LLM observer settings for local experimentation only.
HBDB_LLM_PROVIDER=openrouter
HBDB_LLM_API_KEY=replace-me
HBDB_LLM_MODEL=replace-me
